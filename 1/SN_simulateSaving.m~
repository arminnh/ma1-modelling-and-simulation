function [ path, yield ] = SN_simulateSaving(budget, rate, quartals)
%UNTITLED2 Summary of this function goes here
%   Detailed explanation goes here

    path = budget;
    tax = 0.15;
    
    for i = 2:quartals
        if mod(i,
       path(i) = budget; 
    end

    yield = (path(end) - path(begin)) / budget;
    
end

